{
  "//": "Dedicated typecheck config for the evaluation harness. The root tsconfig EXCLUDES evals/, so without this the runners were never type-checked in CI. Kept separate so the main build/typecheck stays untouched. baseUrl/paths are redefined relative to this directory so the `@/*` alias used by the imported production modules resolves to repo `src/*`.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "baseUrl": "..",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules"]
}
