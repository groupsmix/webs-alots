; Inherit the same peer-dep policy as the root repo. @copilotkit/runtime
; declares @anthropic-ai/sdk@^0.57 as peerOptional, while we pin ^0.101 to
; match the main app's version. Wire-compatible on the surface we use.
; See /.npmrc for the full rationale.
legacy-peer-deps=true
