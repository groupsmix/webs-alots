/**
 * Re-export the unified ErrorBoundary from ui/error-boundary.
 * This file exists for backwards compatibility with existing imports.
 */
export { ErrorBoundary } from "@/components/ui/error-boundary";
